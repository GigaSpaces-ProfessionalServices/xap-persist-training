<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:os-core="http://www.openspaces.org/schema/core"
       xmlns:os-events="http://www.openspaces.org/schema/events"
       xmlns:os-remoting="http://www.openspaces.org/schema/remoting"
       xmlns:tx="http://www.springframework.org/schema/tx"

       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       http://www.openspaces.org/schema/core http://www.openspaces.org/schema/core/openspaces-core.xsd
       http://www.openspaces.org/schema/events http://www.openspaces.org/schema/events/openspaces-events.xsd
       http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd
       http://www.openspaces.org/schema/remoting http://www.openspaces.org/schema/remoting/openspaces-remoting.xsd">

    <!-- DATASOURCE CONFIGURATION -->
    <!-- A JDBC datasource pool that connects to PostgreSQL. -->
    <bean id="dataSource"
          class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="org.postgresql.Driver"/>
        <property name="url" value="jdbc:postgresql://localhost:5432/jbillbuddy"/>
        <property name="username" value="jbillbuddy"/>
        <property name="password" value="Giga1234$"/>
    </bean>

    <!-- Hibernate SessionFactory bean. Uses the pooled data source to connect
        to the database. -->

    <bean id="sessionFactory" class="org.springframework.orm.jpa.hibernate.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="packagesToScan" value="com.c123.billbuddy.model" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.PostgreSQLDialect</prop>
                <!-- "User" is a reserved word in PostgreSQL; quote all identifiers so
                     Hibernate-generated DDL/DML doesn't collide with reserved words. -->
                <prop key="hibernate.globally_quoted_identifiers">true</prop>
                <prop key="hibernate.cache.provider_class">org.hibernate.cache.internal.NoCachingRegionFactory</prop>
                <prop key="hibernate.cache.use_second_level_cache">false</prop>
                <prop key="hibernate.cache.use_query_cache">false</prop>
                <prop key="hibernate.show_sql">false</prop>
                <prop key="hibernate.jdbc.batch_size">1</prop>
                <prop key="hibernate.hbm2ddl.auto">create</prop>
            </props>
        </property>
    </bean>

    <bean id="hibernateSpaceSpaceSynchronizationEndpoint" class="org.openspaces.persistency.hibernate.DefaultHibernateSpaceSynchronizationEndpointFactoryBean">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <bean id="mirrorExceptionHandler" class="com.c123.billbuddy.persistency.BillBuddyMirrorExceptionHandler"/>

    <!-- Wraps the Hibernate sync endpoint so persistence failures (e.g. constraint violations,
         connection errors) route through mirrorExceptionHandler instead of failing silently. -->
    <bean id="exceptionHandlingSpaceSynchronizationEndpoint"
          class="org.openspaces.persistency.patterns.SpaceSynchronizationEndpointExceptionHandler">
        <constructor-arg ref="hibernateSpaceSpaceSynchronizationEndpoint"/>
        <constructor-arg ref="mirrorExceptionHandler"/>
    </bean>

    <!-- MIRROR and SPACE CONFIGURATION The mirror space. Uses the Hibernate
        hibernateSpace Space Synchronization Endpoint. Persists changes done on the Space that connects to
        this mirror space into the database using Hibernate. -->
    <!-- operation-grouping="group-by-replication-bulk" (kept as the mirror default rather than
         switching to group-by-space-transaction) batches operations from multiple unrelated
         transactions/sources together and does not guarantee stable batch composition across
         retries - see BillBuddyMirrorExceptionHandler's javadoc for why that handler tracks retry
         attempts per entry (type+UID) instead of per batch. Docs:
         https://docs.gigaspaces.com/latest/dev-java/asynchronous-persistency-with-the-mirror.html
         https://docs.gigaspaces.com/latest/dev-java/async-persistency-mirror-advanced.html -->
    <os-core:mirror id="mirror" url="/./mirror-service"
                    space-sync-endpoint="exceptionHandlingSpaceSynchronizationEndpoint" operation-grouping="group-by-replication-bulk">
        <os-core:source-space name="BillBuddy-space"
                              partitions="2" backups="1" />
    </os-core:mirror>

</beans>
